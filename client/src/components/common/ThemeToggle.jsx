import React from 'react';

/**
 * Theme toggle removed in favor of single, consistent light theme.
 * Returns null to safely satisfy legacy imports without DOM presence.
 */
const ThemeToggle = () => {
  return null;
};

export default ThemeToggle;
